class Solution {
public:
    vector<int> twoSum(vector<int>& nums, int target) {
        for (int i = 0; i < num.size(); i++) {
            for
                int j = 0;
            j < num.size();j++){
                if (i != j) {
                    if ((num[i] + num[j]) == target) {
                        int arr[2] = [];
                        arr[0] = num[i];
                        arr[1] = num[j];
                        
                    }
                }
            }
        }
    }
}
}
}
;